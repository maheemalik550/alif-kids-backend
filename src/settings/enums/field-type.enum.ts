export enum FieldType {
  TEXT = 'text',
  TEXTAREA = 'textarea',
  NUMBER = 'number',
  BOOLEAN = 'boolean',
  SELECT = 'select',
  MULTISELECT = 'multiselect',
  IMAGE = 'image',
  FILE = 'file',
  RICHTEXT = 'richtext',
  JSON = 'json',
}
